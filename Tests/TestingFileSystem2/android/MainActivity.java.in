package org.chronotext.${PROJECT_NAME};

import android.app.Activity;
import android.content.Intent;
import android.os.Bundle;
import android.util.Log;

public class MainActivity extends Activity
{
  static
  {
    System.loadLibrary("${PROJECT_NAME}");
  }

  @Override
  public void onCreate(Bundle savedInstanceState)
  {
    super.onCreate(savedInstanceState);
    Log.i("MainActivity", "onCreate");
  }

  @Override
  public void onDestroy()
  {
    super.onDestroy();
    Log.i("MainActivity", "onDestroy");
  }

  public boolean testMain(String[] args)
  {
    Log.i("MainActivity", "testMain");

    for (String arg : args)
    {
      System.out.println(arg);
    }

    performTest(); // TODO: PASS ARGUMENTS AND RETURN VALUE
    return true;
  }

  public static native void performTest();
}
