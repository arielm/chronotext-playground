#!/bin/sh

COMPONENT=org.chronotext.${PROJECT_NAME}.tests/org.chronotext.${PROJECT_NAME}.TestRunner

cd tests

if [ $# -eq 0 ];
  then adb shell am instrument -w $COMPONENT
  else adb shell am instrument -w -e args "$@" $COMPONENT
fi

# TODO: EXIT WITH VALUE CORRESPONDING TO TEST RESULTS
